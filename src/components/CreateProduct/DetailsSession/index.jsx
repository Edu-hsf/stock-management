import './styles.scss'

export default function DetailsSession ({ className, form, setForm }) {
    return (
        <div id="detailsSession" className={className}>
            <h1>Details</h1>
        </div>
    )
}