import './styles.scss'

export default function StockSession ({ className, form, setForm }) {
    return (
        <div id="stockSession" className={className}>
            <h1>Stock</h1>
        </div>
    )
}